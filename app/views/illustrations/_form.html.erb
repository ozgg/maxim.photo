<%= form_for entity do |f| %>
    <%= render partial: 'shared/list_of_errors', locals: { entity: entity } %>

    <figure>
      <% unless entity.image.blank? %>
          <%= image_tag entity.image.medium.url, alt: t(:current_image) %>
      <% end %>

      <figcaption>
        <%= f.label :image %>
        <%= f.file_field :image, accept: 'image/jpeg' %>
      </figcaption>
    </figure>

    <div>
      <%= f.label :priority %>
      <%= f.number_field :priority, min: 1, required: true %>
    </div>

    <div>
      <%= f.label :post_id %>
      <%= f.number_field :post_id, min: 1, required: true %>
    </div>

    <div>
      <%= f.label :title %>
      <%= f.text_field :title, required: true %>
    </div>

    <div>
      <%= f.label :description %><br />
      <%= f.text_area :description, cols: 80, rows: 10 %>
    </div>

    <div>
      <%= f.button t(:submit), type: :submit %>
    </div>
<% end %>
