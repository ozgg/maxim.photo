<% content_for :meta_title, t('.title') %>

<article class="illustration">
  <h1><%= t('.title') %></h1>
  <nav class="actions">
    <ul>
      <li><%= link_to t(:list), illustrations_path %></li>
    </ul>
  </nav>

  <%= render partial: 'illustrations/form', locals: { entity: @entity } %>
</article>