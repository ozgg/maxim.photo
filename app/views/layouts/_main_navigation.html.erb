<nav>
  <ul>
    <li><%= link_to t('photos.index.header'), photos_path, accesskey: '1' %></li>
    <li><%= link_to t('index.about.title'), about_path, accesskey: '2' %></li>
    <% if current_user %>
        <li><%= link_to t(:logout), logout_path, method: :delete %></li>
    <% end %>
  </ul>
</nav>
