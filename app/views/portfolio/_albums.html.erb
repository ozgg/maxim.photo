<% if albums.any? %>
    <ul class="albums">
      <% albums.each do |album| %>
          <li><%= render partial: 'portfolio/album_preview', locals: { album: album } %></li>
      <% end %>
    </ul>
<% else %>
    <%= render 'shared/nothing_found' %>
<% end %>
