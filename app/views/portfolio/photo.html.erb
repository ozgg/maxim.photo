<% content_for :meta_title, @photo.name %>

<article class="photo">
  <h1><%= link_to_album @photo.album %> / <%= @photo.name %></h1>

  <figure>
    <%= image_tag @photo.image.big.url, alt: @photo.name %>
  </figure>

  <section>
    <h1><%= t('.other_images') %></h1>

    <%= render partial: 'portfolio/photos', locals: { photos: @album.photos_for_portfolio } %>
  </section>
</article>
