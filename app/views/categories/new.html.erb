<% content_for :title, t('.title') %>

<section class="category">
  <h1><%= t('.title') %></h1>
  <nav>
    <ul>
      <li><%= link_to t(:list), categories_path %></li>
    </ul>
  </nav>

  <%= render 'form' %>
</section>