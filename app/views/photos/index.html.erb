<% content_for :meta_title, t('.title', page: current_page) %>

<article>
  <h1><%= t('.heading') %></h1>
  <% if current_user_has_privilege?(:administrator) %>
      <ul class="actions">
        <li><%= create_icon(new_photo_path) %></li>
      </ul>
  <% end %>

  <%= paginate @collection %>
  <%= render partial: 'list', locals: { collection: @collection } %>
  <%= paginate @collection %>
</article>
