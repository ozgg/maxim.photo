<% content_for :title, t('.title') %>

<section class="photo-form">
  <h1><%= t('.title') %></h1>
  <nav>
    <ul>
      <li><%= link_to t(:list), photos_path %></li>
    </ul>
  </nav>

  <%= render 'form' %>
</section>